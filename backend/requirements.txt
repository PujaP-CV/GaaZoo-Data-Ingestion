# GaaZoo — Unified backend dependencies
# Framework: FastAPI (served via uvicorn)
# Install : pip install -r requirements.txt

# ── Web framework ──────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30       # ASGI server (dev + prod)
python-multipart>=0.0.9       # required for FastAPI form / file uploads
itsdangerous>=2.0             # session signing (used by starlette SessionMiddleware)

# ── Image Pipeline ─────────────────────────────────────────────────────
requests>=2.28
python-dotenv>=1.0
neo4j>=5.0
google-search-results>=2.4
Pillow>=9.0
beautifulsoup4>=4.11
trimesh>=3.23
numpy>=1.24
usd-core>=24.0
# Optional: for full-scene USDZ→GLB (preserves layout). Without it, preview uses pxr+trimesh (can look like a single plank).
aspose-3d>=26.0

# ── DPP (Design Personality Profile) ──────────────────────────────────
google-generativeai>=0.8
openpyxl>=3.1

# ── Shapely layout validation (interior design demo) ─────────────────────
shapely>=2.0

# ── Testing ───────────────────────────────────────────────────────────
pytest>=7.0
httpx>=0.27                  # used by FastAPI TestClient for async request testing

# ── Production ─────────────────────────────────────────────────────────
gunicorn>=21.0                # optional: gunicorn -k uvicorn.workers.UvicornWorker
