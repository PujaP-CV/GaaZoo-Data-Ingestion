[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    integration: tests that need real Neo4j (run with: pytest -m integration)
# Run from backend directory: pytest  or  python -m pytest
